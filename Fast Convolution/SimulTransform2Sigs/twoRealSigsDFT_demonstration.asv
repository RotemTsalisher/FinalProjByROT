fs = 44100; N = 2^10; f0 = 440; f1 = 4400; T = N/fs;
t = (0:N-1)*(1/fs); f_ = linspace(0,fs/2,N);
x1 = sin(2*pi*f0.*t); x2 = sin(2*pi*f1.*t);

[X1_f,X2_f] = twoRealSigsDFT(x1,x2,N);
plot(f_,real(X1_f(1:length(f_))),f_,real(X2_f)); grid on;